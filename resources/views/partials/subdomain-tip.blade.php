{{--<p class="leading-normal">
    <strong>Tip:</strong> try visiting the subdomain for a given service, like <code>aws.consoles.dev</code>, for an even-faster service.<br><br>Replace slashes with dots (so <code>consoles.dev/aws/account</code> would be <code>aws.account.consoles.dev</code>).<br><br>To find the path for any button, hover over it (or tap and hold on mobile) to see its path.
</p>
--}}
